<?xml version="1.0" encoding="windows-1250"?>

<xml>
    {% for u in user %}
    {% for key, line in u %}
        {% if (key == 'birthday') %}
            <{{key}}>{{ line|date('Y-m-d') }}</{{key}}>
        {% elseif (key == 'account_birthday') %}
            <{{key}}>{{ line|date('Y-m-d') }}</{{key}}>
        {% elseif (key == 'influence') %}
            <influences>
            {% for influence in line %}
                <influence>{{ influence }}</influence>
            {% endfor %}
            </influences>
        {% else %}
            <{{key}}>{{line}}</{{key}}>
        {% endif %}
    {% endfor %}
    {% endfor %}
</xml>